apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: traefik-middleware-redirect-www-to-non-www
  namespace: kube-system
spec:
  redirectRegex:
    permanent: true
    regex: ^https?://www\.(.*)
    replacement: https://${1}
